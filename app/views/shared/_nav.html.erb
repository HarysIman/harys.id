<nav class="nav" id="nav" data-controller="nav">
  <div class="nav-inner">
    <a href="<%= root_path %>" class="logo"><span class="logo-mark"></span> Harys.space</a>
    <div class="nav-links" data-nav-target="menu">
      <a href="<%= root_path %>#profil" data-action="click->nav#close">Profil</a>
      <a href="<%= root_path %>#pendekatan" data-action="click->nav#close">Pendekatan</a>
      <a href="<%= root_path %>#karya" data-action="click->nav#close">Karya</a>
      <a href="<%= posts_path %>" data-action="click->nav#close">Blog</a>
      <a href="<%= root_path %>#kontak" data-action="click->nav#close">Kontak</a>
    </div>
    <button class="burger" data-nav-target="burger" data-action="click->nav#toggle" aria-label="Menu">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>
